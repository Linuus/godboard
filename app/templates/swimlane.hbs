{{#x-swimlane swimlane=this deleteAction='deleteSwimlane'}}
  <div class="panel-body">
    {{render 'cards/index' cards }}
  </div>
  <div class="panel-footer">
    {{ render 'cards/new' }}
  </div>
{{/x-swimlane}}
